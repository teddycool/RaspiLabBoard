This is a simple code-lib that shows how to use the RaspiLab hardware from python 2.7.

Each module is a python-class that use one of the hardware features. 

Each file run by itself via the main statement.
 